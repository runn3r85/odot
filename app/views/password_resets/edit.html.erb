<%= title("Change Your Password") %>

<%= form_for @user, builder: FoundationFormBuilder, url: password_reset_path(@user.password_reset_token), html: {method: :patch} do |f| %>
 
  <%= f.password_field :password %>
  <%= f.password_field :password_confirmation %>
  <%= f.submit "Change Password" %>

<% end %>